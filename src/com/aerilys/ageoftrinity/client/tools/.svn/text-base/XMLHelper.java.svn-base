package com.aerilys.ageoftrinity.client.tools;

import com.google.gwt.xml.client.Node;

public class XMLHelper {
  
  public static String getChild(Node node, int index)
  {
    return node.getChildNodes().item(index).getFirstChild().getNodeValue();
  }
}
